node_modules/
dist/
*.log
.DS_Store
.dev.vars
.collateral-config.json

# downloaded helper binaries are cached in ~/.collateral/, never in the repo
bin/

# private developer / prototype notes — not part of the public docs
NOTES.md
