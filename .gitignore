node_modules/
dist/
*.log
.DS_Store
.dev.vars
.collateral-config.json

# downloaded helper binaries are cached in ~/.collateral/, never in the repo
bin/

# private developer / prototype notes - not part of the public docs
NOTES.md

# asciinema raw recordings (keep local; the rendered gif is committed)
*.cast

# local API token, never commit
fizzytoken.txt

# raw screenshots (the committed one is assets/tui.png)
CleanShot*.png
